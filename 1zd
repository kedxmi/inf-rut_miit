# Так как нам прийдется работать с экспоненциальными числами и тригонометрическими функциями, вводим модуль MATH
import math
# Ввод переменных x y z
x = 1.254
y = 1.256
z = 0.845
# Работа с переменной a
krn = 5  # Ввели переменную для дальнейшего извлечения корня 5-й степени из модуля y
krn_y = pow(abs(y), (1/krn))  # Нашли корень 5-й степени модуля y
sin = -3 * math.sin(x)  # Нашли степень экспоненты
a = x ** krn_y + math.exp(sin)  # подставляем полученные данные
print(a)  # Вывод переменной a
# Работа с переменной b
krn = (5 * y) ** 0.5  # Степенной корень z (возвести число в степень 0.5 равноценно корню этого числа).
# так как мы уже закончили работу с переменной a, для меньшей нагрузки на память компьютера использовал krn еще раз
b = math.atan(z ** krn + 1)  # подставляем полученные данные
print(b)  # Вывод переменной b
